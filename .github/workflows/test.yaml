name: Test

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  call-test:
    uses: validator-labs/workflows/.github/workflows/test.yaml@3f2a6b76892b47f2ce520be03b950a456d77ddff
    secrets: inherit
